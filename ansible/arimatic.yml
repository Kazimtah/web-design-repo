---
- name: Play for the ariatheric operator
  gather_facts: false
  hosts: all
  vars:
          y: 72 
          x: 56
  tasks:
          - name: Displaying the valuses
            debug:
                    msg:
                            - "the value of {{x}}"
                            - "the value of {{y}}"
                            - "{{x}} - {{y}} = {{x+y}}"
                            - "{{y}}- {{x}} = {{y-x}}"
                            - "{{x}} * {{y}} = {{y*x}}"  




...
